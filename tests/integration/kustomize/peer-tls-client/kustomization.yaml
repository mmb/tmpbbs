---
components:
  - ../components/test

configmapGenerator:
  - name: tls
    files:
      - ca.crt

namespace: tmpbbs-test-peer-tls-client

patches:
  - path: tmpbbs.statefulset.yaml

replicas:
  - name: tmpbbs
    count: 1

resources:
  - ../../../../kubernetes
