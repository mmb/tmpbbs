---
configmapGenerator:
  - name: tls
    files:
      - tls.crt
      - tls.key

images:
  - name: mm62/tmpbbs
    newName: kind-registry:5000/tmpbbs
    newTag: test

namespace: tmpbbs-test-peer-tls-server

patches:
  - path: tmpbbs.statefulset.yaml

replicas:
  - name: tmpbbs
    count: 1

resources:
  - ../../../../kubernetes
