---
components:
  - ../components/test

configmapGenerator:
  - name: tls
    files:
      - tls.crt
      - tls.key

namespace: tmpbbs-test-peer-tls-server

patches:
  - path: tmpbbs.statefulset.yaml

replicas:
  - name: tmpbbs
    count: 1

resources:
  - ../../../../kubernetes
